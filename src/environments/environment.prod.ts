import { defaultEnvironments } from './enviroment.default';

export const environment = {
  ...defaultEnvironments,
  production: true,
  baseUrl: 'https://ng-http-request-6946e-default-rtdb.firebaseio.com',
};
